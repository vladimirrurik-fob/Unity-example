namespace Otus.Converters.Core
{
    /// <summary>
    /// Identifies a resource converter. Each maps to a <see cref="ConverterConfig"/>.
    /// </summary>
    public enum ConverterId
    {
        Mine = 0,
        Forge = 1,
        Mint = 2,
    }
}
